# Development Dockerfile for Go Gin API Gateway with hot reload
FROM golang:1.23-alpine

# Install air for hot reload (v1.61.5 - compatible with Go 1.23 and has correct module path)
RUN go install github.com/air-verse/air@v1.61.5

# Install git and build dependencies
RUN apk add --no-cache git ca-certificates tzdata

WORKDIR /app

# Copy go mod files
COPY go.mod go.sum ./

# Download dependencies
RUN go mod download

# Copy source code
COPY . .

# Expose port
EXPOSE 8080

# Use air for hot reload
CMD ["air", "-c", ".air.toml"]
